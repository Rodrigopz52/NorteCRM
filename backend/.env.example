# Database URL (SQLite local)
DATABASE_URL="file:./nortecrm.db"

# JWT Secret - ⚠️ CAMBIAR ESTA CLAVE EN PRODUCCIÓN
# Generar una clave segura con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_clave_secreta_aqui

# Puerto del servidor
PORT=3000
